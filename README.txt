Requires zint (http://zint.github.io/).

To run:

python encode.py [FORMAT] STRING
  Where FORMAT is one of:
     --qrcode
     --datamatrix
     --microqr
     --grid
     --aztec
  Default: --qrcode
